name: build-mac

on: [push]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: compile
        run: gcc test.c -o app

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mac-app
          path: app
